server:
  port: 8040

spring:
  application:
    name: order-service
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        username: nacos
        password: nacos
        namespace: public

feign:
  client:
    config:
      product-service: #对应微服务
        loggerLevel: BASIC
        requestInterceptors[0]: com.xunyang.intercepter.feign.CustomFeignIntercepter